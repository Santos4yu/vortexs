# PrizePicks browser helper

This unpacked Chrome/Edge extension reads CS2 cards already visible in the
user's signed-in PrizePicks browser tab. It does not log in, bypass access
controls, or run from the server.

The importer is deliberately confirmation-first: page layouts change and every
captured player, market, and decimal must be reviewed before evaluation.
